import React from 'react';
import PropTypes from 'prop-types';
import Articles from './Articles';
const Home = props => {
  return (
    <div>
      <Articles />
    </div>
  );
};

Home.propTypes = {
  
};

export default Home;